import React, {Component} from 'react';
import PageTitle from "../components/pageTitle";

class JobDetails extends Component {
    render() {
        return (
            <React.Fragment>
                <PageTitle title="Job Details" subtitle="Human Resources Manager"/>
            </React.Fragment>
        );
    }
}

export default JobDetails;
